# MedMNIST-main :
This project encompasses the training of models on the MedMNIST dataset and provides the saved models for each sub-dataset.

# uni-he-main :
This project provides an enhanced version of the uni-he package, incorporating batch normalization and residual block implementations, along with pre-trained models for the MedMNIST dataset.
